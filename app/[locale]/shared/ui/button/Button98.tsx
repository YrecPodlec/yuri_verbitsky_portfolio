import React from 'react';
interface Props {
    text: string;
}
const Button98 = ({text}: Props) => {
    return (
        <button>
            {text}
        </button>
    );
};

export default Button98;